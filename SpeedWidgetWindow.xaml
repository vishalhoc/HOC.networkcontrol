<winex:WindowEx
    x:Class="WinNetControl.SpeedWidgetWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winex="using:WinUIEx"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Speed Widget"
    IsTitleBarVisible="False"
    IsMaximizable="False"
    IsMinimizable="False"
    IsResizable="False"
    Width="200"
    Height="80">

    <winex:WindowEx.SystemBackdrop>
        <winex:TransparentTintBackdrop/>
    </winex:WindowEx.SystemBackdrop>

    <Grid x:Name="RootGrid" Background="{x:Bind BackgroundBrush, Mode=OneWay}" Padding="12" CornerRadius="12">
        <StackPanel Orientation="{x:Bind LayoutOrientation, Mode=OneWay}" Spacing="12" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE898;" FontFamily="Segoe MDL2 Assets" Foreground="Green" Margin="0,0,8,0" FontSize="{x:Bind WidgetFontSize, Mode=OneWay}"/>
                <TextBlock Text="{x:Bind UploadText, Mode=OneWay}" FontWeight="SemiBold" Foreground="White" FontSize="{x:Bind WidgetFontSize, Mode=OneWay}" VerticalAlignment="Center" TextWrapping="NoWrap" TextTrimming="None"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE896;" FontFamily="Segoe MDL2 Assets" Foreground="DeepSkyBlue" Margin="0,0,8,0" FontSize="{x:Bind WidgetFontSize, Mode=OneWay}"/>
                <TextBlock Text="{x:Bind DownloadText, Mode=OneWay}" FontWeight="SemiBold" Foreground="White" FontSize="{x:Bind WidgetFontSize, Mode=OneWay}" VerticalAlignment="Center" TextWrapping="NoWrap" TextTrimming="None"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</winex:WindowEx>
