<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="WinNetControl.InternetResetDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Internet Reset Tools"
    PrimaryButtonText="Run Selected"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    PrimaryButtonClick="OnRunClicked">

    <StackPanel Spacing="8" MinWidth="420" MaxWidth="520">
        <TextBlock Text="Select the operations to perform. Some actions require a system restart to take full effect."
                   TextWrapping="Wrap" Foreground="Gray" FontSize="12" Margin="0,0,0,8"/>

        <!-- Actions -->
        <CheckBox x:Name="ChkFlushDns"    IsChecked="True"  Content="Flush DNS Cache  (ipconfig /flushdns)"/>
        <CheckBox x:Name="ChkResetWinsock" IsChecked="False" Content="Reset Winsock  (netsh winsock reset)  ⚠ Restart needed"/>
        <CheckBox x:Name="ChkResetTcpIp"  IsChecked="False" Content="Reset TCP/IP Stack  (netsh int ip reset)  ⚠ Restart needed"/>
        <CheckBox x:Name="ChkReleaseRenew" IsChecked="False" Content="Release + Renew IP Address  (ipconfig /release + /renew)"/>
        <CheckBox x:Name="ChkFlushArp"    IsChecked="False" Content="Flush ARP Cache  (arp -d *)"/>
        <CheckBox x:Name="ChkResetFW"     IsChecked="False" Content="Reset Windows Firewall to Defaults  ⚠ Removes all WinNetControl rules"/>
        <CheckBox x:Name="ChkClearRules"  IsChecked="False" Content="Clear WinNetControl Firewall Rules Only"/>

        <!-- Output log -->
        <TextBlock Text="Output:" FontWeight="SemiBold" Margin="0,8,0,0"/>
        <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="6" Padding="10">
            <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="OutputLog" TextWrapping="Wrap" FontFamily="Consolas"
                           FontSize="11" Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                           Text="No operations run yet."/>
            </ScrollViewer>
        </Border>
    </StackPanel>
</ContentDialog>
