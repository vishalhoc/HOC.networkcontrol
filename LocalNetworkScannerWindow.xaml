<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinNetControl.LocalNetworkScannerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- toolbar -->
            <RowDefinition Height="Auto"/>  <!-- progress -->
            <RowDefinition Height="Auto"/>  <!-- column headers -->
            <RowDefinition Height="*"/>     <!-- device list -->
            <RowDefinition Height="Auto"/>  <!-- status bar -->
        </Grid.RowDefinitions>

        <!-- ── Toolbar ── -->
        <Grid Grid.Row="0" Padding="12,8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <FontIcon Glyph="&#xE968;" FontSize="18" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                <TextBlock Text="Local Network Scanner" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Border CornerRadius="10" Padding="8,2" Background="{ThemeResource AccentFillColorDefaultBrush}">
                    <TextBlock x:Name="DeviceCountBadge" Text="0 devices" FontSize="11" Foreground="White" FontWeight="SemiBold"/>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                <Button x:Name="ScanButton" Click="OnScanClicked"
                        Style="{StaticResource AccentButtonStyle}" Padding="16,8">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon x:Name="ScanIcon" Glyph="&#xE8B3;" FontSize="14" Foreground="White"/>
                        <TextBlock x:Name="ScanButtonText" Text="Scan Network" FontSize="13" Foreground="White"/>
                    </StackPanel>
                </Button>
                <Button Click="OnBlockAllSelectedClicked" Padding="12,8"
                        Background="#CC3300" ToolTipService.ToolTip="Block all selected devices">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xF140;" FontSize="13" Foreground="White"/>
                        <TextBlock Text="Block Selected" FontSize="12" Foreground="White"/>
                    </StackPanel>
                </Button>
                <Button Click="OnUnblockAllClicked" Padding="12,8"
                        ToolTipService.ToolTip="Unblock all WNC-blocked devices">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE785;" FontSize="13"/>
                        <TextBlock Text="Unblock All" FontSize="12"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- ── Scan Progress ── -->
        <ProgressBar x:Name="ScanProgress" Grid.Row="1" IsIndeterminate="False"
                     Minimum="0" Maximum="100" Value="0"
                     Visibility="Collapsed" Height="4"/>

        <!-- ── Column Headers ── -->
        <Border Grid.Row="2" Margin="12,4,12,0" Padding="12,6"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                CornerRadius="6,6,0,0"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="1,1,1,0">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="28"/>   <!-- checkbox -->
                    <ColumnDefinition Width="22"/>   <!-- status dot -->
                    <ColumnDefinition Width="120"/>  <!-- IP -->
                    <ColumnDefinition Width="160"/>  <!-- Hostname -->
                    <ColumnDefinition Width="140"/>  <!-- MAC -->
                    <ColumnDefinition Width="140"/>  <!-- Vendor / Type -->
                    <ColumnDefinition Width="70"/>   <!-- Latency -->
                    <ColumnDefinition Width="*"/>    <!-- spacer -->
                    <ColumnDefinition Width="100"/>  <!-- Block toggle -->
                </Grid.ColumnDefinitions>
                <CheckBox Grid.Column="0" x:Name="SelectAllCheckBox" Click="OnSelectAllClicked"
                          VerticalAlignment="Center" Margin="0,-2,0,0"/>
                <TextBlock Grid.Column="1"  Text=""             FontSize="11" FontWeight="SemiBold" Foreground="Gray" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2"  Text="IP Address"   FontSize="11" FontWeight="SemiBold" Foreground="Gray" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3"  Text="Hostname"     FontSize="11" FontWeight="SemiBold" Foreground="Gray" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="4"  Text="MAC Address"  FontSize="11" FontWeight="SemiBold" Foreground="Gray" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="5"  Text="Type / Vendor" FontSize="11" FontWeight="SemiBold" Foreground="Gray" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="6"  Text="Ping (ms)"   FontSize="11" FontWeight="SemiBold" Foreground="Gray" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="8"  Text="Block"       FontSize="11" FontWeight="SemiBold" Foreground="Gray" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- ── Device List ── -->
        <Border Grid.Row="3" Margin="12,0,12,4"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="0,0,6,6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="DevicesPanel" Spacing="0"/>
            </ScrollViewer>
        </Border>

        <!-- ── Status bar ── -->
        <Border Grid.Row="4" Margin="12,0,12,8" Padding="12,6"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText"
                           Text="Click 'Scan Network' to discover all devices on your local network."
                           FontSize="11" Foreground="Gray" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="16">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <Ellipse Width="8" Height="8" Fill="#107C10"/>
                        <TextBlock x:Name="OnlineCountText" Text="0 online" FontSize="11" Foreground="Gray"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <Ellipse Width="8" Height="8" Fill="#CC3300"/>
                        <TextBlock x:Name="BlockedCountText" Text="0 blocked" FontSize="11" Foreground="Gray"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
