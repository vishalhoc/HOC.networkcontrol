<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="WinNetControl.Pages.ReportingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="20,14"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Border CornerRadius="10" Padding="10" Background="{ThemeResource LayerFillColorDefaultBrush}">
                        <FontIcon Glyph="&#xE7C3;" FontSize="22" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center" Spacing="2">
                        <TextBlock Text="Network Report" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock Text="Full system snapshot · Export JSON / Markdown / CSV" FontSize="12" Foreground="Gray"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Padding="12,7" Click="OnGenerateReport"
                            Style="{ThemeResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE773;" FontSize="13"/>
                            <TextBlock Text="Generate Snapshot" FontSize="12"/>
                        </StackPanel>
                    </Button>
                    <Button Padding="10,7" Click="OnExportJson">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE898;" FontSize="13"/>
                            <TextBlock Text="Export JSON" FontSize="12"/>
                        </StackPanel>
                    </Button>
                    <Button Padding="10,7" Click="OnExportMarkdown">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE8A5;" FontSize="13"/>
                            <TextBlock Text="Export Markdown" FontSize="12"/>
                        </StackPanel>
                    </Button>
                    <Button Padding="10,7" Click="OnCopyToClipboard">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE8C8;" FontSize="13"/>
                            <TextBlock Text="Copy" FontSize="12"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" ColumnSpacing="12" Margin="16,14,16,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Section selector + metadata -->
            <StackPanel Spacing="12">

                <!-- Report metadata -->
                <Border CornerRadius="12" Padding="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Report Info" FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock x:Name="ReportTimestamp" FontSize="11" Foreground="Gray"/>
                        <TextBlock x:Name="ReportMachine"   FontSize="11" Foreground="Gray"/>
                        <TextBlock x:Name="ReportSections"  FontSize="11" Foreground="Gray"/>
                    </StackPanel>
                </Border>

                <!-- Section checkboxes -->
                <Border CornerRadius="12" Padding="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel Spacing="8">
                        <TextBlock Text="Include Sections" FontSize="13" FontWeight="SemiBold"/>
                        <CheckBox x:Name="SecAdapters"    Content="Network Adapters"    IsChecked="True"/>
                        <CheckBox x:Name="SecConnections" Content="Active Connections"  IsChecked="True"/>
                        <CheckBox x:Name="SecDns"         Content="DNS Configuration"   IsChecked="True"/>
                        <CheckBox x:Name="SecFirewall"    Content="Firewall Summary"     IsChecked="True"/>
                        <CheckBox x:Name="SecRoutes"      Content="Routing Table"        IsChecked="True"/>
                        <CheckBox x:Name="SecProxy"       Content="Proxy Status"         IsChecked="True"/>
                        <CheckBox x:Name="SecSecurity"    Content="Security Summary"     IsChecked="True"/>
                        <CheckBox x:Name="SecSystem"      Content="System Info"          IsChecked="True"/>
                        <CheckBox x:Name="SecLog"         Content="Recent Activity Log"  IsChecked="True"/>
                    </StackPanel>
                </Border>

                <TextBlock x:Name="ReportStatus" Text="" FontSize="11"
                           Foreground="Gray" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Right: Report preview -->
            <Border Grid.Column="1" CornerRadius="12" Padding="0"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Padding="14,10"
                            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                        <Grid>
                            <TextBlock Text="Report Preview" FontSize="13" FontWeight="SemiBold"/>
                            <ProgressBar x:Name="ReportProgress" IsIndeterminate="True"
                                         HorizontalAlignment="Right" Width="120"
                                         Visibility="Collapsed" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto"
                                  Padding="14">
                        <TextBlock x:Name="ReportPreview"
                                   FontFamily="Cascadia Code, Consolas" FontSize="11"
                                   TextWrapping="Wrap" IsTextSelectionEnabled="True"
                                   Text="Click 'Generate Snapshot' to build the report."/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
