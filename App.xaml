<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="WinNetControl.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinNetControl"
    xmlns:core="using:WinNetControl.Core">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters (app-level so all windows can use them without CS1503 in generated code) -->
            <core:BoolToOpacityConverter             x:Key="BoolToOpacityConverter"/>
            <core:SpeedConverter                     x:Key="SpeedConverter"/>
            <core:SizeConverter                      x:Key="SizeConverter"/>
            <core:BoolToVisibilityConverter          x:Key="BoolToVisibilityConverter"/>
            <core:InverseBoolToVisibilityConverter   x:Key="InverseBoolToVisibilityConverter"/>
            <core:BoolToBrushConverter               x:Key="BoolToBrushConverter"/>
            <core:StateToColorConverter              x:Key="StateToColorConverter"/>
            <core:StateToDescriptionConverter        x:Key="StateToDescriptionConverter"/>
            <core:CountToColorConverter              x:Key="CountToColorConverter"/>
            <core:RatioToWidthConverter              x:Key="RatioToWidthConverter"/>
            <core:StringToBoolConverter              x:Key="StringToBoolConverter"/>
            <core:BoolToOnOffConverter               x:Key="BoolToOnOffConverter"/>
            <core:DataUsageTierToBrushConverter      x:Key="DataUsageTierToBrushConverter"/>
            <core:DataUsageTierToVisibilityConverter x:Key="DataUsageTierToVisibilityConverter"/>
            <core:SuspiciousToBrushConverter         x:Key="SuspiciousToBrushConverter"/>
            <core:VtStatusToForegroundConverter      x:Key="VtStatusToForegroundConverter"/>
            <core:SpeedTierToBrushConverter          x:Key="SpeedTierToBrushConverter"/>

            <!-- Globally shared button style: transparent background, no border -->
            <Style x:Key="TransparentButtonStyle" TargetType="Button">
                <Setter Property="Background"           Value="Transparent"/>
                <Setter Property="BorderBrush"          Value="Transparent"/>
                <Setter Property="BorderThickness"      Value="0"/>
                <Setter Property="Padding"              Value="4"/>
                <Setter Property="CornerRadius"         Value="4"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid x:Name="RootGrid" Background="{TemplateBinding Background}"
                                  CornerRadius="{TemplateBinding CornerRadius}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal"/>
                                        <VisualState x:Name="PointerOver">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorSecondaryBrush}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Background">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentPresenter" Storyboard.TargetProperty="Opacity">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="0.4"/>
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter x:Name="ContentPresenter"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  Padding="{TemplateBinding Padding}"
                                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
